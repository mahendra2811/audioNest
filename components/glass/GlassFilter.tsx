/**
 * The clean design drops the SVG refraction/displacement/grain filters.
 * Kept as a no-op so the layout import stays valid and the API is stable.
 */
export function GlassFilter() {
  return null
}
